const btn = document.getElementById("redefinir");

btn.addEventListener("click", () => {
    const cpf = document.getElementById("cpf").value;
    const nascimento = document.getElementById("nascimento").value;
    const senha = document.getElementById("senha").value;
    const senhaConf = document.getElementById("senhaConf").value;

    // Faz o tratamento dos dados, como validar o CPF e verificar se as senhas digitadas são iguais
    // ...

    // Exibe uma mensagem de sucesso ou erro, dependendo dos resultados da validação
    // ...
});